# Super Admin — Issue Register

Generated: 2026-08-17

| ID | Issue | Severity | Root Cause | Status | Fix | Verification | Remaining Risk |
|---|---|---|---|---|---|---|---|
| ISS-001 | Frontend .env pointed to port 8001 (Docker) but local uvicorn runs on 8000 | HIGH | Docker port mapping confusion in `.env` | FIXED | Changed `frontend/.env` to `http://localhost:8000` | Backend health endpoint returns 200 | None |
| ISS-002 | Backend crashes on startup when Neon DB unreachable | HIGH | `initialize_database()` raised unhandled exception, crashing the lifespan | FIXED | Wrapped `initialize_database()` in try/except in `main.py` lifespan; backend starts and returns 503 on DB-dependent endpoints | Verified backend starts with `--host 0.0.0.0 --port 8000` | Backend returns "database: unavailable" on `/health` when Neon is unreachable |
| ISS-003 | UsersPage React runtime crash ("Element type is invalid: got null") | HIGH | `useConfirmationDialog()` returns `{ConfirmationDialog: JSX|null}` but UsersPage rendered it as `<ConfirmationDialog />` (component invocation) instead of `{ConfirmationDialog}` (JSX expression) | FIXED | Changed `<ConfirmationDialog />` to `{ConfirmationDialog}` in `pages/UsersPage.jsx:256` | Frontend build passes; pytest 386/386 | None |
| ISS-004 | DB schema drift: `catalog_version_id` missing from `commercial_subscriptions` | HIGH | Model defined column that didn't exist in live Neon DB; `create_all` only creates tables, never adds columns | FIXED | Added `_add_missing_columns()` to `database.py` using single `information_schema.columns` query; runs on every startup | py_compile passes; will verify on next Neon-reachable startup | Will self-heal on next startup when Neon is reachable |
| ISS-005 | DB schema drift: `actor_role`, `reason`, `correlation_id` missing from `platform_audit_logs` | HIGH | Same root cause as ISS-004 | FIXED | Same `_add_missing_columns()` auto-migration | Same as ISS-004 | Same as ISS-004 |
| ISS-006 | `_add_missing_columns()` too slow against Neon (>60 per-table introspection calls) | MEDIUM | Initial implementation used `inspector.get_columns()` per table, causing 60+ round trips to remote DB | FIXED | Rewrote to use single `SELECT table_name, column_name FROM information_schema.columns WHERE table_schema = 'public'` query | Startup completes in ~2 seconds vs timing out | None |
| ISS-007 | `test_super_admin_lists_commercial_accounts` fails | LOW | Test created org but never provisioned `CommercialAccount`; endpoint only returns existing accounts | FIXED | Added `CommercialAccountService(db_session).ensure_commercial_account(org.id)` to test | `pytest -k test_super_admin_lists_commercial_accounts` passes | None |
| ISS-008 | PlansPage lacks retry button on data load error | MEDIUM | Error banner only had "Dismiss" button, no way to retry | FIXED | Added "Retry" button that calls `load(page)` | Frontend build passes | None |
| ISS-009 | SubscriptionsPage lacks retry button on data load error | MEDIUM | Same as ISS-008 | FIXED | Added "Retry" button that calls `load(page, search)` | Frontend build passes | None |
| ISS-010 | REC-01 — Reconciliation not implemented | CRITICAL | No reconciliation engine, model, scheduled job, or exception-ownership workflow exists for Plane 1 (Zoiko's own commercial billing) | OPEN | NOT IMPLEMENTED — requires building a reconciliation engine; blocked by absence of Plane-1 payment processor (PAY-01/PAY-02 also unimplemented) | Production readiness endpoint reports FAIL | **Blocks production readiness** |
| ISS-011 | SEC-01 dual control not implemented | WARNING | No two-person approval for high-risk single-actor Super Admin actions (kill-switch, MFA admin-reset, org delete) | PARTIAL (updated session 8) | Circuit-breaker changes DO have maker-checker since session 7 (`ApprovalRequest` type `circuit_breaker_change`, self-approval blocked); MFA admin-reset and org deletion remain single-actor | Breaker maker-checker tests (session 7) | Remaining single-actor actions may be acceptable for initial launch with documented risk |
| ISS-012 | Neon DNS resolution intermittent from dev environment | LOW | `getaddrinfo` intermittently fails for `ep-plain-cell-ay3kafa4-pooler.c-5.us-east-2.aws.neon.tech` from local network | KNOWN | Infrastructure issue — works reliably from Docker/cloud environments | — | No risk in production (Docker/CI has reliable DNS) |
| ISS-013 | PlansPage and SubscriptionsPage error banners don't distinguish backend-unavailable from API errors | LOW | No page checks `error.status` for differentiated messaging | OPEN | Would require per-page error classification logic | — | Low user impact |
| ISS-014 | UsersPage lacks standalone Spinner during initial load | LOW | Uses DataTable `loading` prop only, unlike all other pages | OPEN | Minor visual inconsistency | — | No functional impact |
| ISS-015 | `finance_approver`/`auditor` users could not use the app at all | HIGH | `UserRole` gained `FINANCE_APPROVER`/`AUDITOR` (§25 segregation-of-duties, backend-enforced on refund/credit-note/write-off/discount approval routes) but `frontend/src/config/roles.js` and `ProtectedRoute.jsx` were never updated. `VALID_ROLES` excluded both, so `LandingRedirectComp`/`LoginPage` sent an authenticated user with either role straight back to `/login` (session set, nowhere to land) | FIXED | Added both roles to `ROLES`/`ROLE_LABELS`/`ROLE_DEFAULT_REDIRECT` (`roles.js`) and to `ROLE_PATH_RULES` in `ProtectedRoute.jsx`, scoped to `/billing*` (same as `billing_admin`) — the approval/read actions themselves are already gated correctly server-side | Frontend build passes; `ROLE_PATH_RULES` no longer falls through to unrestricted access for unrecognized roles | None — sidebar nav (`BillingShell.jsx`) already renders correctly for any non-`super_admin` role with no changes needed |
| ISS-016 | `POST /billing/invoices/{id}/checkout-session` (Stripe Checkout) always raised `AttributeError` | HIGH | `stripe_service.create_checkout_session()` reads `settings.STRIPE_PAYMENT_METHOD_TYPES` and `settings.STRIPE_BILLING_ADDRESS_COLLECTION`, neither of which existed on the `Settings` class — this crashed regardless of whether Stripe was configured | FIXED | Added both fields to `app/config.py` (`STRIPE_PAYMENT_METHOD_TYPES="card"`, `STRIPE_BILLING_ADDRESS_COLLECTION="auto"`) and documented them in `backend/.env.example` + `README.md` | `app.main` imports cleanly; `settings.STRIPE_PAYMENT_METHOD_TYPES`/`STRIPE_BILLING_ADDRESS_COLLECTION` resolve | The embedded card form on `PublicInvoicePage.jsx` is still an explicit, self-labeled mockup with no `@stripe/stripe-js` installed — real Stripe Elements integration remains a separate, larger, not-yet-started item (see `PAYMENT_GATEWAY_INTEGRATION_GAP_REPORT.md`) |
| ISS-017 | Plane 2 (tenant ledger vs. processor/bank) reconciliation has no model or workflow | MEDIUM (new, not previously tracked) | ISS-010/REC-01 only covers Plane 1 (Zoiko's own commercial billing) reconciliation. ZB-COM-BILL-001 §29/Table 16-17 also requires a Plane 2 `reconciliation_record` (tenant invoices ↔ payments ↔ allocations ↔ tax ↔ accounting export, with owned exceptions) — no such model exists in `billing/models.py` | OPEN | Not implemented — scoping/design deferred to a future session per the 2026-08-21 Plane-2-only discovery pass | — | Does not block current Plane-2 RBAC/Stripe fixes; should be tracked separately from ISS-010 so a future Plane-1 build doesn't get credited for a Plane-2 gap (or vice versa) |
| ISS-018 | `AttentionService.assign()` accepted any `owner_user_id` without checking it referenced a real user | LOW-MEDIUM | Found while writing `docs/SUPER_ADMIN_RBAC_MATRIX.md` (session 4) — the field was passed straight through to the model with no existence check | FIXED | Added a `User` existence check in `attention_service.py:assign()`, raising `NotFoundException` for an unknown ID | `test_attention_assign_rejects_nonexistent_owner` (backend/tests/test_super_admin_command_center.py) | None — this was caught before commit, not a production incident |
| ISS-019 | `AttentionService.assign()` did not advance status from ACKNOWLEDGED to ASSIGNED (only from OPEN) | LOW-MEDIUM | Copy-paste-style logic bug: `if item.status == AttentionStatus.OPEN: item.status = ASSIGNED` didn't account for the ACKNOWLEDGED→ASSIGNED path in the intended lifecycle | FIXED | Condition widened to `if item.status in (OPEN, ACKNOWLEDGED)` | Caught live by `test_attention_operator_lifecycle` during initial authoring (test failed, bug fixed, test re-run green) | None |
| ISS-020 | Granular ABAC permission matrix (ZB-SA-CMD-003 §26) is not ENFORCED — every Command Center endpoint still resolves to the single `super_admin` role only | MEDIUM (design gap, not a regression) | Session 5 shipped declaration-only scaffolding | **FIXED (session 6)** | Added `PlatformRole` (`auth/models.py`, nullable, NULL=full access) + real enforcement in `require_capability()` (`core/capabilities.py`) + `PUT /users/{id}/platform-role` management endpoint (PLATFORM_ADMINISTRATOR-only) | `test_capabilities.py` (16 tests: per-role boundaries, privilege-escalation prevention, immediate revocation, inactive-account rejection) | The ~30 pre-existing Domain A endpoints remain on the bare role check (deliberate, documented scope boundary — see `SUPER_ADMIN_RBAC_MATRIX.md`) |
| ISS-024 | Domain B had no circuit breaker at all — every breaker in the spec's launch catalog was NOT IMPLEMENTED | MEDIUM-HIGH (design gap) | Building a real (non-fake) breaker requires auditing and modifying a live billing-module code path, judged too risky to attempt without a dedicated pass | **PARTIALLY FIXED (session 6)** — "pause invoice finalization" implemented: `InvoiceService.finalize_invoice()` calls `BillingKillSwitchService.require_enabled()`, gated by a new `circuit_breaker.manage` capability + fresh MFA step-up, wired into the real Attention Engine | `test_domain_b_circuit_breaker.py` (8 tests, including calling the billing service directly while paused to prove real enforcement, not just a flag) | 5 of 6 catalog breakers (payment attempts, dunning, communications, connector sync, release freeze) remain NOT IMPLEMENTED — tracked as future work, not hidden |
| ISS-025 | `.env.example` was missing `COMMERCIAL_DUNNING_INTERVAL_MINUTES` (used by `core/scheduler.py`, has a safe `config.py` default of 1440) | LOW | Found during the session-6 standalone-deployment audit | **FIXED (session 6)** | Added to `backend/.env.example` | Manual diff review | None — had a safe default, so this was a documentation-completeness gap, not a functional bug |
| ISS-021 | No tenant-visible privileged-access log (tenant cannot see when a Super Admin accessed their data) | MEDIUM | ZB-SA-CMD-003 §19 explicitly requires this | **FIXED (session 5)** | Added `GET /api/organizations/me/privileged-access-log`, reading `PrivilegedTenantAccessGrant` directly for the caller's own org (org-scoped role, not just super_admin); `PrivilegedAccessLogPage.jsx` frontend | `test_tenant_visible_access_log_shows_own_sessions`, `test_tenant_visible_access_log_cross_tenant_isolation` | None — cross-tenant isolation adversarially tested |
| ISS-022 | TOTP codes could be replayed within their ~90s validity window (a code observed once — shoulder-surfed, logged, intercepted — could complete a second verification, defeating the "fresh authentication" requirement) | HIGH | `pyotp.verify(code, valid_window=1)` alone has no concept of "already used" — found during the Phase 13 security review of `mfa_service.py`, which had zero prior test coverage | **FIXED (session 5)** | Added `SuperAdminMFA.last_used_code_hash`/`last_used_code_at`; `verify_step_up()` rejects a code matching the last-used hash within a 120s window. Session 8 note: login-time MFA was removed entirely, so step-up is now the sole consumer of this protection — and it remains mandatory with no fallback | Replay tests in `backend/tests/test_mfa_service.py` + session-8 suite (`test_breaker_toggle_requires_mfa_even_without_login_mfa`) | None |
| ISS-023 | `mfa_service.py` (Super Admin's step-up-verification module) had zero automated test coverage before session 5 | MEDIUM | Discovered while performing the Phase 13 security review — a genuinely critical authentication path with no regression protection | **FIXED (session 5; updated session 8)** | `backend/tests/test_mfa_service.py` (8 tests), rewritten in session 8 for step-up-only semantics: valid step-up, wrong code, lockout, recovery-code single-use, replay rejection, legitimate-different-code success, step-up-requires-MFA-enabled, enrollment-returns-codes-not-tokens. Plus 26 tests in `test_session8_login_currency_notification.py` covering the privileged paths end-to-end | `pytest tests/test_mfa_service.py` — 8/8 passing | None |
| ISS-026 | Automated WCAG 2.2 AA audit of the Command Center surface had never been run; first axe-core pass found real failures on every audited route (muted `text-slate-400` labels �2.6:1 contrast, white-on-amber/slate P2/P3 severity chips, `text-emerald-600` status text at 3.8:1, footer text below threshold on the dark panel) plus a WCAG 2.2 `target-size` failure on the login password toggle (16�16px clickable area) | MEDIUM-HIGH (�17 gate) | Muted-gray design tokens chosen for aesthetics, never measured against AA ratios; no automated a11y tooling existed in the repo | **FIXED (session 7)** | App-wide token darkening (`text-slate-400`?`text-slate-500`, section labels ?`text-slate-600`, `text-emerald-600`?`text-emerald-700`; `dark:` variants preserved via negative-lookbehind replace), chips switched to dark-on-light, password toggle padded to =24�24 target, footer colors raised (#60A5FA headings, 72% white legal text); committed repeatable auditor `frontend/scripts/a11y-audit.mjs` (Playwright Chromium + axe-core against the real production build with mocked APIs) | Re-run after fixes: **0 violations on all 9 routes** � `docs/a11y-audit-results.json` | Manual screen-reader/AT pass still NOT performed; axe covers automatable rules only |
| ISS-027 | §18.2 super-admin API latency budget (p95 ≤800ms) was asserted nowhere — PERF-01 honestly reported UNKNOWN because nothing measured it | MEDIUM | No timing instrumentation existed for `/api/super-admin/*` | **FIXED (session 7)** | `core/api_metrics.py` sliding-window recorder (2,000 samples / 3,600s) fed by the request-ID middleware; Launch Readiness PERF-01 now reports the MEASURED p95 vs budget (PASS/WARNING at ≥10 samples, honest UNKNOWN below) | `test_api_metrics_p95_math`, `test_perf01_unknown_without_samples` | Frontend complete-lens-render ≤2.0s budget enforced by polling-cadence design but NOT yet measured end-to-end by an automated test |
| ISS-028 | Login-time MFA gate on NORMAL Super Admin logins violated the ZB-SA-CMD-003 v3.0 directive: every super_admin login was forced through an enrollment/challenge screen, and the pending-token side-channel (`mfa_status`/`mfa_token`) was a second, narrowly-scoped JWT flow to maintain | HIGH (directive violation) | Session-5 MFA build placed enforcement at login instead of at privileged actions | **FIXED (session 8)** | `login_user()` now issues real tokens directly to EVERY role; deleted `create_mfa_pending_token`/`decode_mfa_pending_token` (`core/security.py`), `/auth/mfa/enroll/*` + `/auth/mfa/challenge` endpoints, `LoginResponse` schema, and the frontend `SuperAdminMFAGate` component. Enrollment moved to authenticated self-service: `POST /auth/mfa/setup/start|verify`, `GET /auth/mfa/status`, Settings-page card. **Privileged step-up MFA untouched** — activation/breaker/decision endpoints still demand a fresh code with replay protection, lockout, and no fallback | `test_session8_login_currency_notification.py` (login returns tokens for every role; breaker toggle + maker-checker decision still fail closed without a valid fresh code; recovery single-use) | None — step-up remains the sole MFA enforcement point and is adversarially tested |
| ISS-029 | Currency resolution had a silent USD fallback: an unmapped country (or absent country) silently produced USD, and an explicit-but-unsupported currency code was silently swapped — violating the "currency ALWAYS derived from country, NO silent USD fallback" directive | HIGH (directive violation) | Tier-3 default in `resolve_currency()` predated the strict-currency requirement | **FIXED (session 8)** | `resolve_currency()` tier 3 now raises `BadRequestException` naming the country; explicit unsupported codes rejected (never swapped); both creation paths derive-or-reject (`RegisterRequest.currency` and `OrganizationBase.currency` defaults now `None`); `subscription_service._resolve_currency()` terminal fallback = org's own stored currency, never hardcoded USD; `country-defaults` no longer exposes `fallback_currency` | Parametrized tests IN→INR / GB→GBP / US→USD / AU→AUD / DE→EUR; Ghana → explicit 400; countryless org gets NO invented currency; XYZ/GHS rejected at resolver/schema level | Unmapped countries (Ghana/Kenya/Rwanda/Tanzania/Uganda) now REQUIRE an explicit supported currency at creation — intentional fail-loud behavior |
| ISS-030 | Organization creation dispatched NO notification to Super Admins — new orgs were invisible until someone browsed the organizations list, violating the "every successful organization creation triggers a real Super Admin notification email" directive | HIGH (directive violation) | No notification hook existed in either creation path | **FIXED (session 8)** | `notify_super_admins_org_created()` (`services/email_service.py`) + `super_admin_org_created.html` template: recipients resolved server-side (role=super_admin AND is_active), metadata-only content (name/code/country/currency/status/creator/timestamp/deep link), wired post-commit into BOTH `register_enterprise` and `organizations/router.create_organization`; per-recipient failure isolation; fire-and-forget so SMTP failure never fails a committed creation | Tests: recipients = active super admins only; content assertions (no secrets); single-recipient failure isolation; SMTP failure leaves registration successful | Email delivery itself still depends on configured SMTP in the environment (unchanged platform constraint) |